# Routing, Navigation and Contracts

## Next.js routes

| Route | Implementation | Rendering / purpose | Notes |
|---|---|---|---|
| `/` | `src/app/page.tsx` | server redirect | redirects to `/dash` in app code |
| `/dash` | `src/app/dash/page.tsx` | client | Dashboard; `reload=true` is removed after a short delay |
| `/food` | `src/app/food/page.tsx` | client | CalSync; query parameters can open modals |
| `/drinks` | `src/app/drinks/page.tsx` | client | DropSync; `openModal=true` opens the log sheet |
| `/onboarding` | `src/app/onboarding/page.tsx` | client | Intro slides; completion stores `calsync_onboarding_done` |
| `/login` | `src/app/login/page.tsx` | client | Login, registration, MFA, password reset and account management |
| `/support` | `src/app/support/page.tsx` | client | Sends support data to an external API |
| `/legal/privacy` | `src/app/legal/privacy/page.tsx` | server/React | Privacy policy |
| `/legal/cookies` | `src/app/legal/cookies/page.tsx` | server/React | Cookie/consent information |
| `/legal/terms` | `src/app/legal/terms/page.tsx` | server/React | Terms of use |
| `/legal/ai-guidelines` | `src/app/legal/ai-guidelines/page.tsx` | server/React | AI guidelines |
| `/serwist/[path]` | `src/app/serwist/[path]/route.ts` | route handler | Service Worker route generated by Serwist |
| `/api/sync/verify` | `src/app/api/sync/verify/route.ts` | route handler | Verifies access token/user relationship |
| `/api/account/delete` | `src/app/api/account/delete/route.ts` | route handler | Deletes data and the Auth user |
| `/sitemap.xml` | `src/app/sitemap.ts` | metadata route | public index routes defined in code |

Unknown app routes are handled by Next's `not-found.tsx`. `AppShell` renders children for unknown or non-main-app paths but does not add the main navigation.

## Navigation

The bottom navigation is defined in [BottomNav.tsx](../../src/app/_components/navigation/BottomNav.tsx):

```text
Home       -> /dash
Food       -> /food
Hydration  -> /drinks
```

After a tab change it dispatches `viewChanged` with `{ view: 'dashboard' | 'calsync' | 'dropsync' }`.

Global quick actions:

| Action | Target |
|---|---|
| Describe Food | `/food?openModal=true&mode=describe` |
| Import Food | `/food?openModal=true&mode=import` |
| Capture Food | `/food?openModal=true&mode=capture` |
| Search | internal barcode/search popup |
| Scan Barcode | internal popup in camera mode |
| Log Drink | `/drinks?openModal=true` |
| View Workouts | global `WorkoutHistoryModal` |
| View Templates | global `WorkoutModal` |
| Supplements | global `SupplementsModal`, only when enabled |

## Query parameter contracts

### `/dash`

`reload=true` makes the Dashboard page replace itself with `/dash` without query parameters after roughly 2.2 seconds. It is an initialization/refresh hint, not a domain filter.

### `/food`

| Parameter | Expected value | Effect |
|---|---|---|
| `openModal` | `true` | opens the CalSync input modal |
| `mode` | `describe`, `import`, `capture` | selects the AI input method |
| `openAiMethod` | `true` | opens the AI method dialog first |

After handling, `food/page.tsx` removes the processed parameters with `router.replace(..., { scroll: false })`. A `navigate:food` event with `{ openModal: true, mode?: ... }` can request the same state.

### `/drinks`

`openModal=true` opens the DropSync log sheet. The parameter is removed after handling.

### `/login`

`keep_login_page=true` is used for account management so the login/account screen remains visible. `signinginto=healthsync` identifies the login context. The exact presentation remains internal to the page state.

## Own HTTP API

### `POST /api/sync/verify`

Request body:

```json
{
  "accessToken": "<Supabase access token>",
  "userId": "<Supabase user id>"
}
```

The server requires configured Supabase values, creates a client with `Authorization: Bearer <accessToken>`, calls `auth.getUser()` and compares the server-confirmed user ID with `userId`.

| Status | JSON |
|---:|---|
| `200` | `{ "ok": true }` |
| `400` | `{ "ok": false, "error": "Missing accessToken or userId" }` or `Invalid request` |
| `401` | `{ "ok": false, "error": "Unauthorized" }` |
| `403` | `{ "ok": false, "error": "Forbidden" }` |
| `500` | `{ "ok": false, "error": "Supabase not configured" }` |

### `POST /api/account/delete`

Request body:

```json
{
  "accessToken": "<Supabase access token>",
  "userId": "<Supabase user id>"
}
```

The server authenticates the token with the anon client, compares the user ID and then uses the service-role client to delete rows from `calsync_entries`, `dropsync_entries`, `user_settings` and `workout_sessions`, followed by the Auth user.

Responses are `200 { ok: true }`, `400` for an invalid request, `401` for missing/invalid identity and `500` for server/deletion failures. Individual table deletion errors are logged; deletion of the Auth user is still attempted.

## Supabase client contract

The browser client in [supabase.ts](../../src/app/_lib/supabase.ts) uses `createBrowserClient` from `@supabase/ssr` and expects `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`.

Session cookies use `secure`, `sameSite: 'lax'` and a maximum age of 30 days. The database and RLS contract is described in [data-model-and-storage.md](./data-model-and-storage.md).

## External request contracts

| Service | Call | Use | Requirement |
|---|---|---|---|
| Google Gemini | `https://generativelanguage.googleapis.com/v1/models/gemini-3.1-flash-lite:generateContent` | AI nutrition from text/image/camera | AI enabled, terms accepted, third-party consent, user key |
| Gemini model listing | `https://generativelanguage.googleapis.com/v1/models?key=...` | API-key validation | key entry in Settings |
| Open Food Facts Search | `https://search.openfoodfacts.org/search?...` | text search | third-party consent |
| Open Food Facts Product | `https://world.openfoodfacts.org/api/v0/product/<barcode>.json` | barcode lookup | third-party consent and camera/barcode |
| Open-Meteo | `https://api.open-meteo.com/v1/forecast?...` | current weather | preferences + third-party consent, location |
| Nominatim | `https://nominatim.openstreetmap.org/reverse?...` | coordinates to place name | weather location lookup |
| Support API | `https://api.itsmarian.dev/api/healthsync/support` | support form | user action; no local Next proxy |
| Google Analytics | Google Tag Manager/gtag | analytics | analytics consent |

There is no current server-side Gemini proxy in this repository. AI requests go directly from the browser to Google and include the locally stored user key as a query parameter.

## Auth and MFA contract

The login client uses Supabase Auth methods including password sign-in, registration, password reset, password update, sign-out and TOTP factor operations (`listFactors`, `challenge`, `verify`, `enroll`, `unenroll`).

After password login, the Authenticator Assurance Level is checked. If a verified TOTP factor exists and AAL2 is required, the page switches to the MFA view and expects a six-digit code. 2FA setup uses a TOTP factor, QR display and a subsequent challenge/verify flow.

## Rebuild rules

- Remove modal-opening query parameters after processing so a refresh does not repeat the action.
- Treat route actions as idempotent UI signals.
- Check both `response.ok` and the JSON `ok` field.
- Never trust a user ID from a URL; confirm it through the access token.
- Convert external errors into user-friendly states and never log tokens/API keys.

